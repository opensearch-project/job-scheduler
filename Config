# -*-perl-*-

package.Opendistro-job-scheduler = {
    interfaces = (opensearch_2.9.0);

    deploy = {
        generic = true;
    };

    build-environment = {
        chroot = basic;
        network-access = blocked;
    };

    build-system = aws-os-brazil-gradle-plugin-general;
    build-tools = {
        opensearch_2.9.0 = {
            AWSElasticsearchBuildConfig = opensearch_2.9.0;
            AWSElasticsearchBuildScripts = 4.1;
            BrazilPath = 1.1;
            JDK17=1.0;
            BrazilGradle = 7.x;
            JqPackage = 1.x;
            
            OpenSearch = 2.9.0;
        };
    };

    # Use runtime-dependencies for when you want to bring in additional
    # packages when deploying.
    # Use dependencies instead if you intend for these dependencies to
    # be exported to other packages that build against you.
    dependencies = {
        opensearch_2.9.0 = {
        };
    };
};