#!/bin/bash

set -o errexit
set -o pipefail

# clean build dir on every build
rm -rf build/*

mkdir -p build/plugins
unzip -qq "opendistro-job-scheduler-1.4.0.2.zip" -d build/plugins/opendistro-job-scheduler
