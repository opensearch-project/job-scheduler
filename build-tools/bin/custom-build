#!/bin/bash

set -o errexit
set -o pipefail

# clean build dir on every build
rm -rf build/*

mkdir -p build/plugins
unzip -qq "opensearch-job-scheduler-2.6.0.0.zip" -d build/plugins/opendistro-job-scheduler
