#!/bin/bash

set -o errexit
set -o pipefail

# clean build dir on every build
rm -rf build/*

mkdir -p build/plugins
unzip -qq "opensearch-job-scheduler-1.0.0.0-beta1.zip" -d build/plugins/opendistro-job-scheduler
